# Frontend

React + Vite Progressive Web Application for the AI-Based Crop Disease Detection System.

```bash
npm install
npm run dev
```

The development server runs on `http://localhost:5173` and proxies `/api` requests to the Node.js backend on port `3000`.

Useful commands:

```bash
npm test
npm run typecheck
npm run lint
npm run build
```
