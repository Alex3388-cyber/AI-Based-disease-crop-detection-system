{
  "name": "crop-disease-detection-system",
  "version": "1.0.0",
  "private": true,
  "description": "AI-based crop disease detection PWA",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test && npm --prefix frontend run test",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "audit:production": "npm --prefix backend audit --omit=dev && npm --prefix frontend audit --omit=dev"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  }
}
